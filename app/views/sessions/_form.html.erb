<%= form_with(model: session, local: true) do |form| %>
  <% if session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contact_name %>
    <%= form.text_field :contact_name, id: :session_contact_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :session_email %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, id: :session_phone_number %>
  </div>

  <div class="field">
    <%= form.label :timeslot_id %>
    <%= form.text_field :timeslot_id, id: :session_timeslot_id %>
  </div>

  <div class="field">
    <%= form.label :confirmation %>
    <%= form.check_box :confirmation, id: :session_confirmation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
